# Initial Infrastructure Setup
These Open Tofu scripts should be run when the environment is initially setup within AWS. This will create things such as the OIDC provider in AWS so the GitHub actions will be able to create the infrastructure. 

These scripts should be manually run with an account that has high enough permissions to create the appropriate infrastructure.
